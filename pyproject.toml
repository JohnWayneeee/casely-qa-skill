[project]
name = "casely-qa-skill"
version = "2.0.0"
description = "Virtual QA Lead: turns requirement documents into TestRail-ready test cases through one natural conversation."
authors = [{ name = "John Wayne" }]
dependencies = [
    "openpyxl>=3.1.0",
]
requires-python = ">=3.10"

[tool.uv]
managed = true
